{% extends 'layout.twig' %}

{% block content %}
    <ul>
        {% for project in projects %}
            <li><a href="{{ path_for('project', {'project':project}) }}">{{ project }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}
